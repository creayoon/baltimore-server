const assert = require('assert');
const baltimoreServer = require('../index.js');

describe('baltimoreServer', function () {
  it('has a test', function () {
    assert(false, 'baltimoreServer should have a test');
  });
});
